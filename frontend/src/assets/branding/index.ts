import logoPng from './logo.png';

/** Uygulama adı — tarayıcı, PWA ve native etiketlerde aynı kullanılır. */
export const BRAND_NAME = 'Ailem';

/** Logo karesinin koyu zemin rengi (favicon / splash / adaptive icon). */
export const BRAND_SURFACE = '#1A1230';

/** Açılış / light splash zemini. */
export const BRAND_SPLASH_LIGHT = '#FAF9F6';

export const BRAND_LOGO_SRC = logoPng;
