{
  "rules": {
    "voice": {
      "$familyId": {
        ".read": "auth != null && auth.token.family_id === $familyId",
        ".write": "auth != null && auth.token.family_id === $familyId"
      }
    }
  }
}